[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py
pythonpath = src
testpaths = src
addopts = -q --strict-markers
markers =
    slow: teste que demora (parse de PDF grande, OCR)
    llm: teste que chama um provedor de LLM de verdade (NAO roda no CI)
